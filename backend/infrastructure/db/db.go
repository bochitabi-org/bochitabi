package db

// DBClient はDBコネクションを持つ
type DBClient struct {
	// dbconnection
}

// 仮置き
// DBClient はDBClientのコンストラクタ
// func NewDBClient(dbConfig) (*DBClient, error) {
// 	dsn := fmt.Sprintf("%s/%s@....", dbConfig.User, dbConfig.Pass...)
// 	conn, err := db.Open(dsn)
// 	if err != nil {
// 		return nil, err
// 	}

// }
